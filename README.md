# CAN-Bus-Monitor
canbus-monitor is a Linux userspace daemon that captures CAN frames from any SocketCAN-compatible interface (hardware or virtual), writes them to rotating ASC-format log files, and computes per-CAN-ID bus statistics.
